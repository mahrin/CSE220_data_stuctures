class Edge {
    int source;
    int destination;
    Edge next;

    // Constructor
    public Edge(int source, int destination, Edge next) {
        this.source = source;
        this.destination = destination;
        this.next = next;
    }
}